// Webflow Cloud runs on Cloudflare Workers (V8 isolates).
// Follow https://developers.webflow.com/webflow-cloud/environment/framework-customization
// to add any Webflow Cloud-specific configuration required for your deployment.

/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = nextConfig
