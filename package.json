{
  "name": "algolia-webflow",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:sync": "npm run dev --workspace=apps/sync",
    "build:library": "npm run build --workspace=algolia-webflow-library"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
